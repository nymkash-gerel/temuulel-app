# TypeScript incremental cache (regenerated on every run)
.tsc-cache/
# Vitest hook cache
.vitest-cache/
# Local deploy log (when SLACK_WEBHOOK_URL isn't set)
.deploy-log/
# Per-user local hook overrides
*.local.*
